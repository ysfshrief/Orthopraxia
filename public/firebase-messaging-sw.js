/*
  Firebase Cloud Messaging service worker (placeholder).
  To enable true push notifications when the app is CLOSED:
  1) Fill in the firebaseConfig below with your web app keys.
  2) Uncomment the importScripts + initialize block.
  3) In the app, request an FCM token and save it (e.g. to a `fcmTokens`
     collection) so a Cloud Function can target devices.
  Until then, in-app + background reminders work via the Notification API.
*/

// importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js')
// importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-messaging-compat.js')
// firebase.initializeApp({ apiKey:'', authDomain:'', projectId:'', storageBucket:'', messagingSenderId:'', appId:'' })
// const messaging = firebase.messaging()
// messaging.onBackgroundMessage(({ notification }) => {
//   self.registration.showNotification(notification.title, { body: notification.body, icon: '/icon-192.png' })
// })
